<!DOCTYPE html>
<html lang="zh">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>加油奥里给</title>
        <style>
            :root {
                color-scheme: dark;
            }
            * {
                box-sizing: border-box;
            }

            body {
                margin: auto;
                padding: 20px;
                font-family: sans-serif;
                background: linear-gradient(135deg, #1f1c2c, #1f1c2c);
                color: #fff;
                min-height: 100vh;
                min-height: 100dvh;
                max-width: 800px;
            }

            h3,
            h4 {
                text-align: center;
                margin: 10px 0;
            }

            a {
                color: #00f0ff;
                text-decoration: none;
                font-weight: bold;
                transition: all 0.3s ease;
            }

            a:hover {
                text-shadow: 0 0 10px #00f0ff;
            }

            .tips {
                text-align: center;
                margin: 20px 0;
                font-size: 18px;
                color: #ffde59;
            }

            .actions{
                margin-top: 20px;
                padding: 10px;
                background: rgba(255, 255, 255, 0.05);
                border-radius: 12px;
            }

            .list {
                margin-top: 20px;
                padding: 10px;
                background: rgba(255, 255, 255, 0.05);
                border-radius: 12px;
            }

            .list ul {
                list-style: none;
                padding: 0;
            }

            .list li {
                margin: 10px 0;
                padding: 10px;
                background: rgba(255, 255, 255, 0.1);
                border-radius: 8px;
            }

            .power {
                text-align: right;
                margin-top: 30px;
                font-size: 14px;
                color: #a0c4ff;
            }
            b {
                color: #00f0ff;
            }

            @media (max-width: 600px) {
                body {
                    padding: 10px;
                }
            }
            .log {
                margin-top: 20px;
                padding: 10px;
                background: rgba(255, 255, 255, 0.05);
                border-radius: 12px;
            }
            .log textarea {
                width: 100%;
                height: 200px;
                background: rgba(255, 255, 255, 0.1);
                border-radius: 8px;
                border: none;
                color: #fff;
                font-size: 14px;
                resize: vertical;
                font-family: sans-serif;
                outline: none;
                scroll-behavior: smooth;
            }

            .list table{
                width: 100%;
                table-layout: fixed;
                border: 1px solid #00f0ff;
                border-collapse: collapse;
            }
            .list table thead{
                background-color: #2f1c2c;
            }
            .list table th{
                border: 1px solid #00f0ff;
                text-align: center;
            }
            .list table td{
                border: 1px solid #00f0ff;
                text-align: center;
            }
            .list table td:first-child{
                font-weight: bold;
            }
        </style>
    </head>
    <body>
        <h3>🌟 状态查询 🌟</h3>

        <h4>没有你的名字？点击👉<a href="/login">这里👈</a>一键登录</h4>

        <div class="tips">共有<%= total %>名老师正在学习中👇</div>
        <div class="actions">
            过滤：
            <a href="?status=all">全部</a>
            <a href="?status=finish">已完成</a>
            <a href="?status=unfinish">未完成</a>
        </div>
        <div class="list">
            <table>
                        <thead>
                            <tr>
                                <th>老师名称</th>
                                <th>已选课程</th>
                                <th>已完成课程</th>
                                <th>未完成课程</th>
                                <th>已选学分</th>
                                <th>已获得学分</th>
                                <th>读书笔记</th>
                                <th>反思周记</th>
                                <th style="width: 110px;">操作</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% data.forEach(item=> { %>
                            <tr>
                                <td><%= item.realName %></td>
                                <td><%= item.courseInfo?.TotalCount %></td>
                                <td><%= item.courseInfo?.EndCount %></td>
                                <td><%= item.courseInfo?.NotEndCount %></td>
                                <td><%= item.courseInfo?.totalPeriod %></td>
                                <td><%= item.courseInfo?.obtainedValue %></td>
                                <td><%= item.bookCredit %></td>
                                <td><%= item.weeklyCredit %></td>
                                <td>
                                    <a target="_blank" href="https://www.cdsjxjy.cn/cdcte/#/tokenLogin?token=<%= item.token %>">登录</a> 
                                    <% if (item.bookCredit != 8 || item.weeklyCredit != 8) { %>
                                        <a target="_blank" href="/write?token=<%= item.token %>&username=<%= item.username %>">填写</a>
                                    <% } %>
                                    <a target="_blank" href="#" style="color: red;" onclick="handleDelete('<%= item.username %>',event)">删除</a>
                                </td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
        </div>

        <div class="log">
            <textarea name="" id="" readonly></textarea>
        </div>

        <div class="power">--- develop by 帅哥程序猿 💻</div>
        <script>
            const ws = new WebSocket("/");
            ws.onmessage = (e) => {
                const log = document.querySelector(".log textarea");
                log.innerHTML += e.data;
            };
            function handleDelete(username,event) {
                event.preventDefault();
                if (confirm("确定要删除吗？")) {
                    window.location.href = `/delete?username=${username}`;
                }
            }
        </script>
    </body>
</html>
