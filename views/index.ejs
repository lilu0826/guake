<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>加油奥里给</title>
    <style>
        @font-face {
            font-family: 'Orbitron';
            font-style: normal;
            font-weight: 500;
            font-display: swap;
            src: url(/public/Orbitron.woff2) format('woff2');
            unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
        }
        * {
            box-sizing: border-box;
        }

        body {
            margin: auto;
            padding: 20px;
            font-family: 'Orbitron', sans-serif;
            background: linear-gradient(135deg, #1f1c2c, #1f1c2c);
            color: #fff;
            min-height: 100vh;
            min-height: 100dvh;
            max-width: 500px;
        }

        h3, h4 {
            text-align: center;
            margin: 10px 0;
        }

        a {
            color: #00f0ff;
            text-decoration: none;
            font-weight: bold;
            transition: all 0.3s ease;
        }

        a:hover {
            text-shadow: 0 0 10px #00f0ff;
        }

        .tips {
            text-align: center;
            margin: 20px 0;
            font-size: 18px;
            color: #ffde59;
        }
        .list {
            margin-top: 20px;
            padding: 10px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 12px;
        }

        .list ul {
            list-style: none;
            padding: 0;
        }

        .list li {
            margin: 10px 0;
            padding: 10px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 8px;
        }

        .power {
            text-align: right;
            margin-top: 30px;
            font-size: 14px;
            color: #a0c4ff;
        }
        b{
            color: #00f0ff;
        }

        @media (max-width: 600px) {
            body {
                padding: 10px;
            }
        }
    </style>
</head>
<body>
    <h3>🌟 状态查询 🌟</h3>

    <h4>
        没有你的名字？点击👉<a href="/login">这里👈</a>一键登录
    </h4>

    <div class="tips">共有<%= data.length %>名老师正在学习中👇</div>

    <div class="list">
        <ul>
            <% data.forEach(item=> { %>
                <li><b><%=item.realName%></b>老师<br><%=item.tips%>
                </li>
            <% }) %>
        </ul>
    </div>
    <div class="power">--- develop by 帅哥程序猿 💻</div>
</body>
</html>